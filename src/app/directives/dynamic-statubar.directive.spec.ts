import { DynamicStatubarDirective } from './dynamic-statubar.directive';

describe('DynamicStatubarDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicStatubarDirective();
    expect(directive).toBeTruthy();
  });
});
